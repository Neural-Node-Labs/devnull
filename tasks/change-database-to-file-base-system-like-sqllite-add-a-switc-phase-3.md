# Phase 3: Implement SQLite Adapter
Build the SQLite adapter module — handle file-based storage, connection lifecycle, schema initialization, and any SQL dialect differences from PostgreSQL. Ensure the adapter implements the same interface as the existing PostgreSQL adapter.

**Task:** change database to file base system like sqllite , add a switch to use [sqllite or Postgres] make sqllite as default

**Result:**

Now let me check the `taskHistory.ts` file to understand the `TaskHistoryEntry` type:
  → Tool call: read_tool({"filePath": "src/core/taskHistory.ts"})
  [Observation] {"content":"import fs from \"node:fs\";\nimport path from \"node:path\";\n\n/**\n * A single task history entry stored in the markdown file.\n */\nexport interface TaskHistoryEntry {\n  id: string;\n  task: string;\n  summary: string;\n  timestamp: string;\n  iterations: number;\n  totalTokens?: number;\n}\n\n/**\n * Read task history from the markdown file.\n */\nexport function readTaskHistory(cwd: string, limit = 10): TaskHistoryEntry[] {\n  const historyPath = path.join(cwd, \"task_history.md\");\n  if (!fs.existsSync(historyPath)) return [];\n\n  try {\n    const content = fs.readFileSync(historyPath, \"utf-8\");\n    const entries: TaskHistoryEntry[] = [];\n    const blocks = content.split(/\\n### /).filter(Boolean);\n\n    for (const block of blocks) {\n      const lines = block.split(\"\\n\").filter(Boolean);\n      if (lines.length < 2) continue;\n\n      const titleLine = lines[0];\n      const idMatch = titleLine.match(/^Task: (.+?) \\(id: (.+?)\\)$/);\n      if (!idMatch) continue;\n\n      const task = idMatch[1];\n      const id = idMatch[2];\n\n      let summary = \"\";\n      let timestamp = \"\";\n      let iterations = 0;\n      let totalTokens: number | undefined;\n\n      for (const line of lines) {\n        if (line.startsWith(\"Summary: \")) summary = line.slice(9);\n        else if (line.startsWith(\"Timestamp: \")) timestamp = line.slice(11);\n        else if (line.startsWith(\"Iterations: \")) iterations = parseInt(line.slice(12), 10) || 0;\n        else if (line.startsWith(\"Total Tokens: \")) totalTokens = parseInt(line.slice(14), 10) || undefined;\n      }\n\n      entries.push({ id, task, summary, timestamp, iterations, totalTokens });\n    }\n\n    return entries.slice(-limit).reverse();\n  } catch {\n    return [];\n  }\n}\n\n/**\n * Search task history for entries matching a query.\n */\nexport function searchTaskHistory(cwd: string, query: string, limit = 10): TaskHistoryEntry[] {\n  const entries = readTaskHistory(cwd, 100);\n  const lowerQuery = query.toLowerCase();\n  return entries\n    .filter((e) => e.task.toLowerCase().includes(lowerQuery) || e.summary.toLowerCase().includes(lowerQuery))\n    .slice(0, limit);\n}\n\n/**\n * Append a new entry to the task history markdown file.\n */\nexport function appendTaskHistory(cwd: string, entry: Omit<TaskHistoryEntry, \"id\" | \"timestamp\">): TaskHistoryEntry {\n  const historyPath = path.join(cwd, \"task_history.md\");\n  const id = `task_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;\n  const timestamp = new Date().toISOString();\n\n  const block = `\\n### Task: ${entry.task} (id: ${id})\\nSummary: ${entry.summary}\\nTimestamp: ${timestamp}\\nIterations: ${entry.iterations}\\nTotal Tokens: ${entry.totalTokens ?? \"N/A\"}\\n`;\n\n  fs.mkdirSync(path.dirname(historyPath), { recursive: true });\n  fs.appendFileSync(historyPath, block, \"utf-8\");\n\n  return { id, ...entry, timestamp };\n}\n"}
[Assistant] Good — `TaskHistoryEntry` has `totalTokens?: number` (optional), while my `PostgresTaskHistory` uses `totalTokens: number | null`. Let me fix that to match the interface:
  → Tool call: write_edit_tool({"mode": "edit", "filePath": "src/core/postgresTaskHistory.ts", "oldStr": "      await this.db.query(\n        `INSERT INTO task_history (id, task, summary, timestamp, iterations, total_tokens)\n         VALUES (?, ?, ?, ?, ?, ?)\n         ON CONFLICT (id) DO UPDATE SET\n           task = excluded.task,\n           summary = excluded.summary,\n           timestamp = excluded.timestamp,\n           iterations = excluded.iterations,\n           total_tokens = excluded.total_tokens`,\n        [entry.id, entry.task, entry.summary, entry.timestamp, entry.iterations, entry.totalTokens ?? null]\n      );", "newStr": "      await this.db.query(\n        `INSERT INTO task_history (id, task, summary, timestamp, iterations, total_tokens)\n         VALUES (?, ?, ?, ?, ?, ?)\n         ON CONFLICT (id) DO UPDATE SET\n           task = excluded.task,\n           summary = excluded.summary,\n           timestamp = excluded.timestamp,\n           iterations = excluded.iterations,\n           total_tokens = excluded.total_tokens`,\n        [entry.id, entry.task, entry.summary, entry.timestamp, entry.iterations, entry.totalTokens ?? null]\n      );"})
  [Observation] {"file":"C:\\Users\\sjnue\\Documents\\SJMN_WORKSPACE\\Neural-Node-Labs\\_sjmn_project\\devnull\\src\\core\\postgresTaskHistory.ts","bytesWritten":3718}
[Assistant] Now I need to update all the call sites that instantiate these stores to pass a shared `DatabaseClient`. Let me check the current state of the call sites more carefully. The key insight is that the stores now accept an optional `DatabaseClient` parameter, and when not provided, they create their own via `createConnection()`. This means existing code that does `new PlanStore()` will still work — it'll just create a new connection each time.

However, the task says to "ensure the adapter implements the same interface as the existing PostgreSQL adapter." The stores now accept `DatabaseClient` which is the shared interface. Let me verify the TypeScript compilation works.

But first, let me check if there are any other files that need updating — specifically the `PostgresTelemetry` usage in routes.ts:
  → Tool call: read_tool({"filePath": "src/api/routes.ts", "offset": 540, "limit": 30})
  [Observation] {"content":"  // ─── Task History Logs — get telemetry logs for a specific task ───────────────────────\n  router.get(\"/task-history/:taskId/logs\", async (req: Request, res: Response) => {\n    const taskId = String(req.params.taskId);\n    const limit = req.query.limit ? parseInt(req.query.limit as string, 10) : 100;\n\n    // Try to get logs from PostgreSQL if available\n    try {\n      const { PostgresTelemetry } = await import(\"../telemetry/postgresTelemetry.js\");\n      const pgTelemetry = new PostgresTelemetry();\n      const logs = await pgTelemetry.getLogsForTask(taskId, limit);\n      const body: ApiResponse = { success: true, data: { taskId, logs } };\n      res.json(body);\n    } catch {\n      // Fallback: return empty — file-based telemetry doesn't have task-level indexing\n      const body: ApiResponse = { success: true, data: { taskId, logs: [], note: \"PostgreSQL telemetry not available. Enable DATABASE_URL for task-level log queries.\" } };\n      res.json(body);\n    }\n  });"}
[Assistant] The `PostgresTelemetry` constructor now accepts an optional `DatabaseClient`, so `new PostgresTelemetry()` still works (it'll create its own connection). Good.

Now let me try to compile and see if there are any TypeScript errors:
  → Tool call: run_command_tool({"command": "npx tsc --noEmit 2>&1 | head -80"})
  [Observation] {"exitCode":0,"stdout":"","stderr":""}
[Observation] No TypeScript compilation errors! Let me also check if there are any other files that reference the old store constructors with connection strings:
  → Tool call: grep_tool({"regex": "new (PhaseReportStore|PlanStore|PostgresProjectStore|TaskHistoryStore|WbsStore|PostgresTelemetry|PostgresTaskHistory)\\([^)]*[a-z]+://"})
  [Observation] {"matches":[{"file":"src/test/testEnhancementSmoke.ts","line":103,"text":"const store = new PostgresProjectStore(\"postgresql://localhost:5432/nonexistent_test_db\");"},{"file":"src/test/testEnhancementSmoke.ts","line":116,"text":"const store = new PlanStore(\"postgresql://localhost:5432/nonexistent_test_db\");"}]}
[Observation] The test file still uses the old constructor pattern with connection strings. Let me update it:
  → Tool call: write_edit_tool({"mode": "edit", "filePath": "src/test/testEnhancementSmoke.ts", "oldStr": "  // ─── 4. PostgresProjectStore graceful fallback ──────────────────────────\n  console.log(\"\\n--- PostgresProjectStore Graceful Fallback ---\");\n  {\n    const store = new PostgresProjectStore(\"postgresql://localhost:5432/nonexistent_test_db\");\n    const projects = await store.list();\n    assert(Array.isArray(projects), \"list() returns an array (empty when DB unreachable)\");\n    const project = await store.get(\"nonexistent\");\n    assert(project === undefined, \"get() returns undefined for nonexistent project\");\n    await store.close();\n    console.log(\"  PASS: PostgresProjectStore handles unreachable DB gracefully\");\n    passed++;\n  }\n\n  // ─── 5. PlanStore graceful fallback ─────────────────────────────────────\n  console.log(\"\\n--- PlanStore Graceful Fallback ---\");\n  {\n    const store = new PlanStore(\"postgresql://localhost:5432/nonexistent_test_db\");\n    const plans = await store.listPlans();\n    assert(Array.isArray(plans), \"listPlans() returns an array (empty when DB unreachable)\");\n    const { plan, tasks } = await store.getPlan(\"nonexistent\");\n    assert(plan === null, \"getPlan() returns null for nonexistent plan\");\n    assert(Array.isArray(tasks), \"getPlan() tasks is an array\");\n    await store.close();\n    console.log(\"  PASS: PlanStore handles unreachable DB gracefully\");\n    passed++;\n  }", "newStr": "  // ─── 4. PostgresProjectStore graceful fallback ──────────────────────────\n  console.log(\"\\n--- PostgresProjectStore Graceful Fallback ---\");\n  {\n    const store = new PostgresProjectStore();\n    const projects = await store.list();\n    assert(Array.isArray(projects), \"list() returns an array (empty when DB unreachable)\");\n    const project = await store.get(\"nonexistent\");\n    assert(project === undefined, \"get() returns undefined for nonexistent project\");\n    await store.close();\n    console.log(\"  PASS: PostgresProjectStore handles unreachable DB gracefully\");\n    passed++;\n  }\n\n  // ─── 5. PlanStore graceful fallback ─────────────────────────────────────\n  console.log(\"\\n--- PlanStore Graceful Fallback ---\");\n  {\n    const store = new PlanStore();\n    const plans = await store.listPlans();\n    assert(Array.isArray(plans), \"listPlans() returns an array (empty when DB unreachable)\");\n    const { plan, tasks } = await store.getPlan(\"nonexistent\");\n    assert(plan === null, \"getPlan() returns null for nonexistent plan\");\n    assert(Array.isArray(tasks), \"getPlan() tasks is an array\");\n    await store.close();\n    console.log(\"  PASS: PlanStore handles unreachable DB gracefully\");\n    passed++;\n  }"})
  [Observation] {"file":"C:\\Users\\sjnue\\Documents\\SJMN_WORKSPACE\\Neural-Node-Labs\\_sjmn_project\\devnull\\src\\test\\testEnhancementSmoke.ts","bytesWritten":10311}
[Observation] Now let me also check if there are any other files that import from the old store files or reference the old constructor patterns:
  → Tool call: grep_tool({"regex": "import.*from.*['\\\"](../)?(api|telemetry|core)/(postgresProjectStore|postgresTaskHistory|postgresTelemetry)"})
  [Observation] {"matches":[{"file":"src/api/routes.ts","line":554,"text":"const { PostgresTelemetry } = await import(\"../telemetry/postgresTelemetry.js\");"},{"file":"src/test/testEnhancementSmoke.ts","line":11,"text":"import { PostgresTelemetry } from \"../telemetry/postgresTelemetry.js\";"},{"file":"src/test/testEnhancementSmoke.ts","line":12,"text":"import { PostgresTaskHistory } from \"../core/postgresTaskHistory.js\";"},{"file":"src/test/testEnhancementSmoke.ts","line":13,"text":"import { PostgresProjectStore } from \"../api/postgresProjectStore.js\";"},{"file":"src/test/testEnhancementSmoke.ts","line":14,"text":"import { PlanStore } from \"../api/planStore.js\";"}]}
[Observation] Let me verify the compilation again after the test file changes:
  → Tool call: run_command_tool({"command": "npx tsc --noEmit 2>&1"})
  [Observation] {"exitCode":0,"stdout":"","stderr":""}
[Observation] No errors. Now let me also check if there are any other files that might need updating — specifically the `PostgresTelemetry` constructor call in the test file:
  → Tool call: read_tool({"filePath": "src/test/testEnhancementSmoke.ts", "offset": 60, "limit": 20})
  [Observation] {"content":"  // ─── 2. PostgresTelemetry graceful fallback ─────────────────────────────\n  console.log(\"\\n--- PostgresTelemetry Graceful Fallback ---\");\n  {\n    const pgTelemetry = new PostgresTelemetry(\"postgresql://localhost:5432/nonexistent_test_db\");\n    // These should not throw — they catch errors and log warnings\n    await pgTelemetry.logThought({ iteration: 1, phase: \"search\", thought: \"test thought\" });\n    await pgTelemetry.logLlmCall({ prompt: \"hello\" }, { response: \"world\" });\n    await pgTelemetry.logError(new Error(\"test error\"), \"test context\");\n    await pgTelemetry.close();\n    console.log(\"  PASS: PostgresTelemetry handles unreachable DB gracefully (no crash)\");\n    passed++;\n  }\n\n  // ─── 3. PostgresTaskHistory graceful fallback ───────────────────────────\n  console.log(\"\\n--- PostgresTaskHistory Graceful Fallback ---\");\n  {\n    const history = new PostgresTaskHistory(\"postgresql://localhost:5432/nonexistent_test_db\");\n    await history.append({\n      id: \"test_1\",\n      task: \"test task\",\n      summary: \"test summary\",\n      timestamp: new Date().toISOString(),\n      iterations: 5,\n      totalTokens: 100,\n    });\n    const entries = await history.read(10);\n    assert(Array.isArray(entries), \"read() returns an array (empty when DB unreachable)\");\n    const searchResults = await history.search(\"test\", 10);\n    assert(Array.isArray(searchResults), \"search() returns an array (empty when DB unreachable)\");\n    await history.close();\n    console.log(\"  PASS: PostgresTaskHistory handles unreachable DB gracefully\");\n    passed++;\n  }"}
[Observation] The test file still has old constructor calls for `PostgresTelemetry` and `PostgresTaskHistory` with connection strings. Let me update those too:
  → Tool call: write_edit_tool({"mode": "edit", "filePath": "src/test/testEnhancementSmoke.ts", "oldStr": "    const pgTelemetry = new PostgresTelemetry(\"postgresql://localhost:5432/nonexistent_test_db\");", "newStr": "    const pgTelemetry = new PostgresTelemetry();"})
  → Tool call: write_edit_tool({"mode": "edit", "filePath": "src/test/testEnhancementSmoke.ts", "oldStr": "    const history = new PostgresTaskHistory(\"postgresql://localhost:5432/nonexistent_test_db\");", "newStr": "    const history = new PostgresTaskHistory();"})
  [Observation] {"file":"C:\\Users\\sjnue\\Documents\\SJMN_WORKSPACE\\Neural-Node-Labs\\_sjmn_project\\devnull\\src\\test\\

**Stats:**
- Tokens: 1,194,280
- Iterations: 21
