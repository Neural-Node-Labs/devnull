# Phase 2: Architecture & Packaging Strategy Design
Design the packaging approach: choose between compiled binary (e.g., PyInstaller, Nuitka), container image (Docker), or platform-specific installer (e.g., .deb, .rpm, .exe). Define the artifact structure — what goes in, what stays out (source code excluded), how configuration is externalized, and how secrets/environment variables are handled. Produce `blueprint.md` and `solution-design.md` with the chosen strategy and tradeoffs.

**Task:** create a plan how we will package the software with out the sources code and with capability to install and setup write in DEPLOYMENT_PLAN.md

**Result:**

I see the problem — the `solution-design.md` file was overwritten with the old Phase Planning content instead of the new Packaging Strategy content. The initial write via `node -e` wrote the new content, but then the `appendFileSync` appended to the *existing* file which already had the old Phase Planning content. Let me fix this by writing the correct content.

Let me write the correct solution-design.md with the packaging strategy content:
  → Tool call: run_command_tool({"command": "node -e \"\nconst fs = require('fs');\nconst content = '# Solution Design: devnull Packaging & Artifact Strategy\\n\\n## 1. Purpose\\n\\nThis document details the solution design for packaging devnull into deployable artifacts that exclude source code, externalize configuration, handle secrets securely, and support the target deployment environments.\\n\\n## 2. Packaging Strategy Overview\\n\\n### 2.1 Primary Artifact: Docker Container Image\\n\\n**Format:** OCI-compatible Docker image (devnull-api:latest, devnull-ui:latest)\\n\\n**Rationale:**\\n- The project already has a working multi-stage Dockerfile and docker-compose.yml\\n- The docker_deploy_ssh_tool already implements remote Docker deployment with pre-checks, rollback, and health verification\\n- Docker provides clean isolation, dependency management, and reproducible builds\\n- Multi-service stack (API + UI + Postgres) is naturally expressed via Docker Compose\\n- Industry-standard CI/CD integration\\n\\n### 2.2 Secondary Artifact: Deploy Tarball\\n\\n**Format:** devnull-deploy.tar.gz (gzipped tar archive)\\n\\n**Rationale:**\\n- Air-gapped environments without Docker registry access\\n- Manual inspection/modification before deployment\\n- Fallback when docker_deploy_ssh_tool is not available\\n- Contains everything needed to docker compose up -d --build on the target\\n\\n### 2.3 Rejected Alternatives\\n\\n| Alternative | Reason for Rejection |\\n|---|---|\\n| Compiled binary (pkg/nexe) | Poor ESM support; struggles with native modules (ssh2, pg); large binaries (~50MB+); doesnt eliminate Node.js dependency |\\n| Platform installer (.deb/.rpm/.exe) | Multi-service stack cant be expressed as a single package; would need fpm + repo management; platform-specific build matrix |\\n| Single fat image with UI | API and UI have different lifecycle, scaling, and update requirements; separate images allow independent management |\\n\\n## 3. Artifact Structure\\n\\n### 3.1 API Docker Image (devnull-api:latest)\\n\\n/opt/devnull/ (DEVNULL_HOME)\\n  dist/ - Compiled TypeScript (JS only)\\n    cli/index.js - CLI entry point\\n    core/ - Orchestrator, skill registry, protocol\\n    tools/ - 12 tool implementations\\n    api/ - Express API server\\n    llm/ - DeepSeek client\\n    config/ - LLM config loader\\n    indexing/ - Workspace indexer\\n    telemetry/ - File-based logging\\n    remote/ - SSH/SCP utilities\\n    test/ - Test suites (optional)\\n  agent/ - Skills, protocol, config (fallback)\\n    devnull.md - Engineering protocol\\n    config/llm.yaml - LLM backend config\\n    skills/ - 13 hot-plug skills\\n  package.json - Production dependencies manifest\\n  package-lock.json - Locked dependency versions\\n  .env.example - Template for users .env\\n  node_modules/ - Production dependencies only\\n\\n**Included:** dist/, agent/, package.json, package-lock.json, node_modules/ (production only), .env.example\\n\\n**Excluded:** src/ (TypeScript source), dev dependencies, .env files (secrets), .git/, .log/, .agent/index/, tasks/, reports/, coverage/, tests/, e2e/, ui-test-suited/, ui/ (separate image), scripts/, enhancement/, docs/\\n\\n### 3.2 UI Docker Image (devnull-ui:latest)\\n\\n/usr/share/nginx/html/\\n  index.html - SPA entry point\\n  assets/ - Built JS/CSS bundles\\n\\n/etc/nginx/conf.d/default.conf - nginx config with API proxy\\n\\n**Included:** Built Vite output, nginx config with API reverse proxy\\n**Excluded:** ui/src/ (React source), ui/node_modules/, build config files\\n\\n### 3.3 Deploy Tarball (devnull-deploy.tar.gz)\\n\\ndevnull-deploy.tar.gz\\n  dist/ - Compiled JS\\n  Dockerfile - Multi-stage build\\n  docker-compose.yml - API + UI + Postgres\\n  .dockerignore - Build context exclusions\\n  .env.example - Template (NOT .env)\\n  package.json - Production dependencies\\n  package-lock.json - Locked versions\\n  tsconfig.json - TypeScript config (reference)\\n  agent/ - Skills, protocol, config\\n  ui/ - Frontend source (Dockerfile, nginx.conf, package.json, built assets)\\n\\n## 4. Build Pipeline\\n\\n### 4.1 Build Stages\\n\\nSource (TypeScript) -> npm run build (tsc) -> dist/ (compiled JS)\\n  -> Docker Build (multi-stage)\\n    -> Stage 1: builder (node:20-alpine, npm install all deps, tsc compile)\\n    -> Stage 2: runtime (node:20-alpine, apk add system deps, npm install --omit=dev, copy dist/ from builder, copy agent/, set DEVNULL_HOME, create non-root user, HEALTHCHECK, ENTRYPOINT)\\n  -> Image Tag & Push (devnull-api:{version}, devnull-api:latest)\\n\\n### 4.2 UI Build Pipeline\\n\\nui/src/ (React/TypeScript) -> npm run build (Vite) -> ui/dist/ (static assets)\\n  -> Docker Build (multi-stage)\\n    -> Stage 1: builder (node:20-alpine)\\n    -> Stage 2: serve (nginx:alpine, copy built assets, copy nginx.conf, expose port 80)\\n  -> Image Tag & Push\\n\\n### 4.3 Deploy Tarball Creation\\n\\nnode scripts/create-deploy-tarball.mjs\\nbash scripts/create-deploy-tarball.sh\\n\\nOutput: devnull-deploy.tar.gz\\n\\nThe tarball creation scripts:\\n1. Include: dist/, Dockerfile, docker-compose.yml, .dockerignore, .env.example, package.json, package-lock.json, tsconfig.json, agent/, ui/\\n2. Exclude: .env, .env.local, .env.production, node_modules/, .git/, .log/, .agent/index/, tasks/, reports/, coverage/, test artifacts\\n3. Validate: Check all required files present, verify .env is NOT included\\n4. Report: File count, total size, missing files\\n\\n## 5. Configuration Externalization\\n\\n### 5.1 Environment Variables (Runtime)\\n\\nAll configuration is externalized via environment variables, passed at container runtime:\\n\\n| Variable | Default | Purpose | Sensitivity |\\n|---|---|---|---|\\n| DEEPSEEK_API_KEY | (required) | DeepSeek LLM access | SECRET |\\n| ANTHROPIC_API_KEY | (optional) | Fallback LLM provider | SECRET |\\n| GITHUB_TOKEN | (optional) | GitHub API auth for github_tool | SECRET |\\n| DEVNULL_API_KEY | (unset) | API Bearer token auth | SECRET |\\n| DATABASE_URL | postgresql://devnull:devnull_pass@localhost:5432/devnull | PostgreSQL connection | Contains password |\\n| DEVNULL_API_PORT | 3001 | API server port | Non-sensitive |\\n| DEVNULL_API_HOST | 0.0.0.0 | API server bind address | Non-sensitive |\\n| DEVNULL_HOME | /opt/devnull | Fallback path for agent skills/config | Non-sensitive |\\n| NODE_ENV | production | Runtime environment | Non-sensitive |\\n| POSTGRES_USER | devnull | PostgreSQL user | Non-sensitive |\\n| POSTGRES_PASSWORD | devnull_pass | PostgreSQL password | SECRET |\\n| POSTGRES_DB | devnull | PostgreSQL database name | Non-sensitive |\\n| REMOTE_SSH_USER | (optional) | SSH user for remote deploy | Credential |\\n| REMOTE_SSH_PASSWORD | (optional) | SSH password for remote deploy | SECRET |\\n| MAX_ITERATIONS | 10 | ReAct loop iteration ceiling | Non-sensitive |\\n\\n### 5.2 Configuration Files (Baked into Image)\\n\\nThe following configuration files are baked into the Docker image as fallbacks:\\n\\n- agent/devnull.md - Engineering protocol (can be overridden by workspace mount)\\n- agent/config/llm.yaml - LLM model selection, per-skill reasoning overrides (can be overridden)\\n- agent/skills/*/SKILL.md - 13 built-in skills (can be overridden by workspace mount)\\n\\n**Override mechanism:** When a workspace is mounted at /workspace, any agent/ directory in the workspace takes precedence over the images built-in agent/. This allows projects to ship custom skills, protocols, or LLM config without modifying the image.\\n\\n### 5.3 Secret Handling\\n\\n| Concern | Approach |\\n|---|---|\\n| API keys | Passed via --env-file .env at docker run; never baked into image |\\n| Database credentials | Passed via environment variables; DATABASE_URL contains password |\\n| SSH credentials | Passed via environment variables (REMOTE_SSH_USER, REMOTE_SSH_PASSWORD) |\\n| GitHub token | Passed via GITHUB_TOKEN env var; used in-memory only |\\n| .env file | Explicitly excluded from Docker build via .dockerignore |\\n| .env in tarball | Explicitly excluded from deploy tarball via exclusion rules |\\n| Runtime secrets | Never logged to telemetry files (.log/); only non-sensitive metadata |\\n\\n## 6. Deployment Modes\\n\\n### 6.1 Docker CLI Mode (Single Container)\\n\\ndocker run --rm -it -v \\\"\\$PWD\\\":/workspace --env-file .env devnull:latest --task \\\"fix the bug\\\"\\n\\n### 6.2 Docker Compose (Full Stack)\\n\\ndocker compose --profile serve up -d\\nStarts: api (port 3001) + ui (port 8080) + postgres (port 5432)\\n\\n### 6.3 Remote Deploy (via docker_deploy_ssh_tool)\\n\\nUses the built-in tool to package, ship, and deploy:\\n1. Creates tarball of workspace\\n2. Ships via SCP to remote host\\n3. Runs docker compose up -d --build\\n4. Verifies health endpoints\\n5. Supports rollback on failure\\n\\n### 6.4 Local Development (No Docker)\\n\\nnpm install\\nnpm run build\\nnode dist/cli/index.js --task \\\"...\\\"   (CLI mode)\\nnode dist/cli/index.js --serve        (API server mode)\\n\\n## 7. Versioning & Tagging\\n\\n| Artifact | Tag Strategy | Example |\\n|---|---|---|\\n| Docker image (API) | devnull-api:{version}, devnull-api:latest | devnull-api:0.2.0 |\\n| Docker image (UI) | devnull-ui:{version}, devnull-ui:latest | devnull-ui:0.2.0 |\\n| Deploy tarball | devnull-deploy-{version}.tar.gz | devnull-deploy-0.2.0.tar.gz |\\n| Git tag | v{version} | v0.2.0 |\\n\\n## 8. Rollback Strategy\\n\\n### 8.1 Docker Image Rollback\\n\\ndocker compose down\\ndocker compose -f docker-compose.yml up -d --build  (rebuilds from current source)\\ndocker pull devnull-api:{previous-version}           (pull previous image)\\ndocker tag devnull-api:{previous-version} devnull-api:latest\\ndocker compose up -d                                 (restart with previous image)\\n\\n### 8.2 Deploy Tarball Rollback\\n\\nKeep previous tarball: devnull-deploy-{previous-version}.tar.gz\\nExtract and deploy:\\n  tar xzf devnull-deploy-{previous-version}.tar.gz -C /opt/devnull-rollback\\n  cd /opt/devnull-rollback\\n  docker compose up -d --build\\n\\n### 8.3 Database Rollback\\n\\nPostgreSQL data is stored in a Docker volume (pgdata). To roll back:\\n  docker compose down\\n  docker volume rm devnull_pgdata\\n  docker compose up -d\\n  (Restore from backup if available)\\n\\n## 9. Security Considerations\\n\\n| Concern | Mitigation |\\n|---|---|\\n| Source code exposure | TypeScript compiled to JS; source maps included but no .ts files shipped |\\n| Secret leakage | .env excluded from image and tarball; secrets passed at runtime |\\n| Container escape | Non-root user (devnull); no --privileged flag |\\n| Supply chain | package-lock.json pins dependency versions; multi-stage build isolates build tools |\\n| Network exposure | API binds to configurable host/port; healthcheck is unauthenticated by design |\\n| SSH credentials | Passed via env vars, never written to disk in the image |\\n\\n## 10. CI/CD Integration\\n\\n### 10.1 GitHub Actions Workflow\\n\\nname: Build and Deploy\\non:\\n  push:\\n    branches: [main]\\n  pull_request:\\n    branches: [main]\\n\\njobs:\\n  build:\\n    runs-on: ubuntu-latest\\n    steps:\\n      - uses: actions/checkout@v4\\n      - uses: actions/setup-node@v4\\n        with:\\n          node-version: 20\\n      - run: npm ci\\n      - run: npm run build\\n      - run: npm test\\n\\n  docker:\\n    needs: build\\n    runs-on: ubuntu-latest\\n    steps:\\n      - uses: actions/checkout@v4\\n      - name: Build API image\\n        run: docker build -t devnull-api:\\${{ github.sha }} .\\n      - name: Build UI image\\n        run: docker build -t devnull-ui:\\${{ github.sha }} ./ui\\n      - name: Create deploy tarball\\n        run: node scripts/create-deploy-tarball.mjs\\n      - name: Upload tarball\\n        uses: actions/upload-artifact@v4\\n        with:\\n          name: devnull-deploy\\n          path: devnull-deploy.tar.gz\\n\\n### 10.2 Pre-deploy Validation\\n\\nBefore deploying to production:\\n1. Run unit tests (vitest)\\n2. Run E2E tests (playwright)\\n3. Build Docker images\\n4. Run container smoke tests (healthcheck)\\n5. Create rollback snapshot\\n6. Deploy with health verification\\n\\n## 11. Artifact Verification\\n\\n### 11.1 Docker Image Verification\\n\\ndocker build -t devnull-api:test .\\ndocker run --rm devnull-api:test --help    (verify CLI works)\\ndocker run --rm devnull-api:test --skills  (verify 13 skills loaded)\\ndocker run --rm devnull-api:test --serve & (verify API starts)\\ncurl http://localhost:3001/api/v1/health   (verify health endpoint)\\n\\n### 11.2 Deploy Tarball Verification\\n\\ntar -tzf devnull-deploy.tar.gz | sort      (list contents)\\nCheck required files present:\\n  dist/ (has .js files)\\n  Dockerfile\\n  docker-compose.yml\\n  .dockerignore\\n  .env.example\\n  package.json\\n  package-lock.json\\n  tsconfig.json\\n  agent/ (has skills)\\n  ui/ (has Dockerfile)\\nVerify .env is NOT in tarball\\n\\n### 11.3 Source Code Exclusion Verification\\n\\nVerify no .ts files in dist/:\\n  find dist/ -name \\\"*.ts\\\" | wc -l   (should be 0)\\nVerify no src/ in image:\\n  docker run --rm devnull-api:test ls /opt/devnull/src   (should fail)\\n';\nfs.writeFileSync('solution-design.md', content, 'utf8');\nconsole.log('Written ' + content.length + ' bytes');\n\"\n"})
  [Observation] {"command":"node -e \"\nconst fs = require('fs');\nconst content = '# Solution Design: devnull Packaging & Artifact Strategy\\n\\n## 1. Purpose\\n\\nThis document details the solution design for packaging devnull into deployable artifacts that exclude source code, externalize configuration, handle secrets securely, and support the target deployment environments.\\n\\n## 2. Packaging Strategy Overview\\n\\n### 2.1 Primary Artifact: Docker Container Image\\n\\n**Format:** OCI-compatible Docker image (devnull-api:latest, devnull-ui:latest)\\n\\n**Rationale:**\\n- The project already has a working multi-stage Dockerfile and docker-compose.yml\\n- The docker_deploy_ssh_tool already implements remote Docker deployment with pre-checks, rollback, and health verification\\n- Docker provides clean isolation, dependency management

**Stats:**
- Tokens: 1,372,073
- Iterations: 21
